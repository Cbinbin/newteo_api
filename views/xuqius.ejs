<!DOCTYPE html>
<html>
<head>
	<!-- <link rel="stylesheet" type="text/css" href="../storage/css/xuqius.css" /> -->
	<style type="text/css">
		body { padding: 100px 25px; }
		h1 { float: left; 
			margin: 10px; 
			width: 70%; 
			font-size: 1.5rem; }
		p { float: left; 
			margin: 10px; 
			width: 70%; 
			font-size: 2rem; }
		.rim { float: left; 
			margin: 20px; 
			padding: 50px; 
			border-style: inset; 
			border-width: 14px; 
			border-color: #FF7744; }
		button { float: right; 
			width: 18%; 
			height: 60px; 
			font-size: 2rem; 
			border-radius: 2rem; 
			box-shadow: 3px 3px 10px #373a3c; 
			outline: none; 
			cursor: pointer; }
	</style>
	<script type="text/javascript">
		function firm(id) {
			// var id = document.querySelector('h1').getAttribute('data-item-id')
			var token = document.querySelector('h1').getAttribute('data-tt')
			var tf = confirm("你确定删除这个需求吗？")
			if(tf) {
				fetch('https://api.newteo.com/requirement/'+id+'?token='+token, { method: 'DELETE'})
				.then((res)=> {
					alert("已删除")
					location.href = 'https://api.newteo.com/xuqiu'
				})
				.catch((err)=> {
					alert(err)
				})
			}
		}
	</script>
</head>
<body>
	<% for(var i=0; i<data.length; i++) { %>
		<div class= "rim">
			<h1 data-item-id= "<%= data[i]._id %>" data-tt= "<%= tt %>">ID: <%= data[i]._id %></h1>
			<p>称呼: <%= data[i].name %></p>
			<p>电话: <%= data[i].phone %></p>
			<p>公司: <%= data[i].company %></p>
			<p>需求: <%= data[i].info %></p>
			<p>时间: <%= data[i].create_time %></p>
			<button onclick="firm('<%= data[i]._id %>')">删除</button>
		</div>
		<br/>
	<% } %>
</body>
</html>
